# keenetic_manager_app

Flutter application shell for Keenetic Deck.

Initial responsibilities:

- app bootstrap
- navigation
- adaptive layouts
- screen wiring for shared use cases

This package is intentionally small at the start. Router behavior should live in shared packages, not in the UI layer.
